<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.marketing.dal.mysql.asmrequirement.AsmRequirementMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <!-- 批量插入（Oracle 使用 INSERT ALL） -->
    <insert id="insertBatch" parameterType="list">
        INSERT ALL
        <foreach collection="list" item="item" separator=" ">
            INTO marketing_asm_requirement (
            id, assembly_material_no, main_material_desc,
            require_quantity, require_date,
            host_unit, vehicle_model,
            creator, create_time, updater, update_time, deleted, tenant_id
            ) VALUES (
            #{item.id, jdbcType=BIGINT},
            #{item.assemblyMaterialNo, jdbcType=VARCHAR},
            #{item.mainMaterialDesc, jdbcType=VARCHAR},
            #{item.requireQuantity, jdbcType=DECIMAL},
            #{item.requireDate, jdbcType=TIMESTAMP},
            #{item.hostUnit, jdbcType=VARCHAR},
            #{item.vehicleModel, jdbcType=VARCHAR},
            #{item.creator, jdbcType=VARCHAR},
            #{item.createTime, jdbcType=TIMESTAMP},
            #{item.updater, jdbcType=VARCHAR},
            #{item.updateTime, jdbcType=TIMESTAMP},
            0,
            1
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>
</mapper>