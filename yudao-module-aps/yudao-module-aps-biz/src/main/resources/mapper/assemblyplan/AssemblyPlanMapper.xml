<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.aps.dal.mysql.assemblyplan.AssemblyPlanMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <!-- AssemblyPlanMapper.xml -->
    <!-- 批量插入（Oracle 使用 INSERT ALL） -->
    <insert id="insertBatch" parameterType="list">
        INSERT ALL
        <foreach collection="list" item="item" separator=" ">
            INTO aps_assembly_plan (
            id, order_no, material_code, material_desc,
            assembly_quantity, assembled_quantity,
            schedule_time, workshop, import_time,
            create_time, update_time, creator, updater, deleted, tenant_id
            ) VALUES (
            #{item.id}, #{item.orderNo}, #{item.materialCode}, #{item.materialDesc},
            #{item.assemblyQuantity}, #{item.assembledQuantity},
            #{item.scheduleTime}, #{item.workshop}, #{item.importTime},
            SYSDATE, SYSDATE, #{item.creator}, #{item.updater}, 0, 1
            )
        </foreach>
        SELECT * FROM dual
    </insert>

    <delete id="deleteByUniqueKey">
        DELETE FROM aps_assembly_plan
        WHERE order_no = #{orderNo}
          AND schedule_time = #{scheduleTime}
          AND workshop = #{workshop}
    </delete>
</mapper>