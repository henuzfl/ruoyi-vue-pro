<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.wm.dal.mysql.realtimestock.SapStockImportMapper">

    <!-- 这里可以定义具体的SQL语句 -->
    <!--
    <insert id="insertOrUpdateFromSap">
        INSERT INTO wm_stock (item_code, quantity, warehouse_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.code}, #{item.quantity}, #{item.warehouseId})
        </foreach>
        ON DUPLICATE KEY UPDATE
            quantity = VALUES(quantity)
    </insert>
    -->

</mapper>