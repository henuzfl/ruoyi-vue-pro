<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.wm.dal.mysql.orderdemand.OrderDemandMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <!-- 批量删除：根据 (order_no, material_no) 组合 -->
    <delete id="deleteByOrderNoAndMaterialNos" parameterType="list">
        DELETE FROM TRACE_ORDER_DEMAND
        WHERE (order_no) IN
        <foreach collection="keys" item="key" open="(" separator="," close=")">
            (#{key.orderNo})
        </foreach>
    </delete>

    <!-- 批量插入（使用 INSERT ALL 语法，适配 Oracle） -->
    <insert id="batchInsert" parameterType="list">
        INSERT ALL
        <foreach collection="list" item="item" index="index">
            INTO trace_order_demand (
            id, order_no, trace_demand_no, material_no, material_description,
            demand_quantity, outbound_accumulated, open_quantity, status, remark,
            creator, create_time, updater, update_time, deleted, tenant_id
            ) VALUES (
            #{item.id, jdbcType=NUMERIC},
            #{item.orderNo, jdbcType=VARCHAR},
            #{item.traceDemandNo, jdbcType=VARCHAR},
            #{item.materialNo, jdbcType=VARCHAR},
            #{item.materialDescription, jdbcType=VARCHAR},
            #{item.demandQuantity, jdbcType=NUMERIC},
            #{item.outboundAccumulated, jdbcType=NUMERIC},
            #{item.openQuantity, jdbcType=NUMERIC},
            #{item.status, jdbcType=NUMERIC},
            #{item.remark, jdbcType=VARCHAR},
            #{item.creator, jdbcType=VARCHAR},
            #{item.createTime, jdbcType=TIMESTAMP},
            #{item.updater, jdbcType=VARCHAR},
            #{item.updateTime, jdbcType=TIMESTAMP},
            0,
            1
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>
</mapper>