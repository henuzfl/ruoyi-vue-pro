<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.buyer.dal.mysql.codeconfig.CodeConfigMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <insert id="batchInsert">
        INSERT ALL
        <foreach collection="list" item="item" index="index">
            INTO buyer_code_config (
            id, name, host_code, teli_code,
            create_time, update_time, creator, updater, deleted, tenant_id
            ) VALUES (
            #{item.id, jdbcType=NUMERIC},
            #{item.name, jdbcType=VARCHAR},
            #{item.hostCode, jdbcType=VARCHAR},
            #{item.teliCode, jdbcType=VARCHAR},
            #{item.createTime, jdbcType=TIMESTAMP},
            #{item.updateTime, jdbcType=TIMESTAMP},
            #{item.creator, jdbcType=VARCHAR},
            #{item.updater, jdbcType=VARCHAR},
            0,
            1
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <delete id="deleteByHostCodes">
        DELETE FROM buyer_code_config WHERE host_code IN
        <foreach collection="hostcodes" item="hostcode" open="(" separator="," close=")">#{hostcode}</foreach>
    </delete>

</mapper>