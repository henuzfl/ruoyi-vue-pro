<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.buyer.dal.mysql.overseasinventory.OverseasInventoryMapper">
    <delete id="deleteAllPhysical">
        DELETE FROM buyer_overseas_inventory
    </delete>

    <insert id="batchInsert">
        INSERT ALL
        <foreach collection="list" item="item">
            INTO buyer_overseas_inventory (
                id, warehouse, owner_code, supplier_code, supplier_name,
                item_code, item_name, item_specification, inventory_quantity,
                occupied_quantity, available_quantity, frozen_quantity,
                creator, create_time, updater, update_time, deleted, tenant_id
            ) VALUES (
                #{item.id, jdbcType=NUMERIC}, #{item.warehouse, jdbcType=VARCHAR},
                #{item.ownerCode, jdbcType=VARCHAR}, #{item.supplierCode, jdbcType=VARCHAR},
                #{item.supplierName, jdbcType=VARCHAR}, #{item.itemCode, jdbcType=VARCHAR},
                #{item.itemName, jdbcType=VARCHAR}, #{item.itemSpecification, jdbcType=VARCHAR},
                #{item.inventoryQuantity, jdbcType=NUMERIC}, #{item.occupiedQuantity, jdbcType=NUMERIC},
                #{item.availableQuantity, jdbcType=NUMERIC}, #{item.frozenQuantity, jdbcType=NUMERIC},
                #{item.creator, jdbcType=VARCHAR}, #{item.createTime, jdbcType=TIMESTAMP},
                #{item.updater, jdbcType=VARCHAR}, #{item.updateTime, jdbcType=TIMESTAMP}, 0, 1
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>
</mapper>
