<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.buyer.dal.mysql.productiontransfer.ProductionTransferMapper">

    <!-- 批量插入（使用 INSERT ALL，兼容 Oracle） -->
    <insert id="batchInsert">
        INSERT ALL
        <foreach collection="list" item="item" index="index">
            INTO buyer_production_transfer (
            id, order_no, material_code, material_desc, production_scheduler,
            transfer_initiator, initiator_date, quantity, transfer_no, batch_no,
            signer, sign_time, create_time, update_time, creator, updater, deleted, tenant_id
            ) VALUES (
            #{item.id, jdbcType=NUMERIC},
            #{item.orderNo, jdbcType=VARCHAR},
            #{item.materialCode, jdbcType=VARCHAR},
            #{item.materialDesc, jdbcType=VARCHAR},
            #{item.productionScheduler, jdbcType=VARCHAR},
            #{item.transferInitiator, jdbcType=VARCHAR},
            #{item.initiatorDate, jdbcType=TIMESTAMP},
            #{item.quantity, jdbcType=NUMERIC},
            #{item.transferNo, jdbcType=VARCHAR},
            #{item.batchNo, jdbcType=VARCHAR},
            #{item.signer, jdbcType=VARCHAR},
            #{item.signTime, jdbcType=TIMESTAMP},
            #{item.createTime, jdbcType=TIMESTAMP},
            #{item.updateTime, jdbcType=TIMESTAMP},
            #{item.creator, jdbcType=VARCHAR},
            #{item.updater, jdbcType=VARCHAR},
            0,
            1
            )
        </foreach>
        SELECT 1 FROM DUAL
    </insert>

    <!-- 根据转序单号列表物理删除 -->
    <delete id="deleteByTransferNos">
        DELETE FROM buyer_production_transfer
        WHERE transfer_no IN
        <foreach collection="transferNos" item="transferNo" open="(" separator="," close=")">
            #{transferNo}
        </foreach>
    </delete>

    <!-- 根据转序单号与计划批次列表物理删除 -->
    <delete id="deleteByTransferNosAndBatchNos">
        DELETE FROM buyer_production_transfer
        WHERE (transfer_no, batch_no) IN (
        <foreach collection="list" item="item" separator=" UNION ALL ">
            SELECT #{item.transferNo}, #{item.batchNo} FROM DUAL
        </foreach>
        )
    </delete>
</mapper>